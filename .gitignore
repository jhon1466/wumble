# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# ───────────────────────── SECRETS — never commit ─────────────────────────
# Firebase config, rules, functions & hosting cache (security: keep local only)
.firebaserc
firebase.json
.firebase/
firestore.rules
firestore.indexes.json
storage.rules
functions/
**/google-services.json
**/GoogleService-Info.plist
lib/firebase_options.dart
admin-dashboard/lib/firebase.ts

# API keys / local secrets
dart_defines.json
lib/core/config/app_secrets.dart
**/*.env
**/.env
**/.env.*

# Android signing
**/key.properties
**/*.jks
**/*.keystore
!**/debug.keystore

# ───────────────────────── Not part of the published app ─────────────────────
# Next.js admin panel and Flutter web target are kept local only.
admin-dashboard/
web/

# Local build/debug junk
**/build/
/android/.gradle/
android/build_error.txt
android/dependencies_report.txt
signing_report.txt
*.webp.bak
